import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  CustomFullWidth: {
    width: '100%',
  },
  buttonSize: {
    width: '140Px',
    height: '40px',
  },
  errorColor: {
    color: 'red',
  },
}));

export { useStyles };
